import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "192.168.1.11",
    "next-store-juan-antes-projects.vercel.app",
  ],
};

export default nextConfig;
